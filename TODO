
TODO:

** add loading indicator on document action button
   while waiting for notifications

** fix seen notifications

** Close path back to records,
   remove final flag

* add close action for records
* email interface
* confirm receive with sms

* add env option to disable emailing,texting
* export data
* office to office messaging
* account management
* setup https
* handle slow connections
* dynamic update route table
* follow/subscribe to routes
* recent/active document list
* js: listen for notifications
* email/sms notifications
* fix logout
    autologout: check even if tab is not active
    delete cookies when browser is closed

* add facebook notifications

* editable routes

* set more than one attachments and notes/annotations
    ** Add model: Note(id, trackingId, officeId, contents)

--------------------------------------------

* num of days/minutes
* subject searching
* show loading indicators
* page queries
* handle deletion (or prevent)

--------------------------------------------
- restrict API access



